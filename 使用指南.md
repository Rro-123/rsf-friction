# 速率-状态摩擦库 (rsf.js) 使用指南

> 目标场景：把本库嵌入一个「仿真摩擦力模拟实验」，用户只需调整 **接触面材质**、**接触面受压力**、**物体运动速度** 三个变量，就能得到对应的摩擦力。

---

## 一、快速开始（一行代码）

```js
// 引入库（浏览器 <script src="rsf.js"></script> 后，或 Node require）
const RSF = window.RSF;          // 浏览器
// const RSF = require('./rsf.js'); // Node

// 三个变量 → 摩擦力
const 材质 = 'granite';   // 接触面材质
const 压力 = 1000;        // 法向力 N = 1000 牛顿
const 速度 = 1e-5;        // 1e-5 m/s (0.01 mm/s)

const 结果 = RSF.computeFriction(材质, 压力, 速度);
console.log(结果.frictionForce);  // 摩擦力 (N)
console.log(结果.mu);             // 摩擦系数 μ
```

就这么简单：**材质 + 法向力 + 速度 → 摩擦力**。

---

## 二、三个变量如何映射到库的输入

### 1. 接触面材质 → 摩擦参数

库内置了一个**材质预设表** `RSF.materials`，每个材质名对应一组摩擦参数：

```js
RSF.materials.granite
// { name:'花岗岩（岩石）', mu:0.60, muS:0.65,
//   mu0:0.60, a:0.008, b:0.012, Dc:5e-6, V0:1e-6 }
```

材质分两类：

| 类型 | 含义 | 使用的模型 |
|---|---|---|
| **岩石类**（granite / sandstone / limestone） | 含 RSF 参数 `mu0, a, b, Dc, V0` | 自动用 **RSF 模式**（精确、含速度依赖） |
| **工程材料**（steel / wood / rubber / ptfe …） | 只含库仑 `mu` / `muS` | 自动用 **库仑模式**（简单） |

自定义材质也支持直接传参数对象：

```js
// 自定义 RSF 材质（岩石类）
const 我的材质 = { mu0:0.6, a:0.01, b:0.015, Dc:1e-5, V0:1e-6 };

// 自定义库仑材质（工程类）
const 我的金属 = { mu:0.5, muS:0.65 };

RSF.computeFriction(我的材质, 1000, 1e-5);
RSF.computeFriction(我的金属, 1000, 0.5);
```

### 2. 接触面受压力 → 法向力 N

摩擦力公式是 `F = μ·N`，其中 `N` 是**法向力（正压力）**，单位 **牛顿 (N)**。

如果你的实验给的是**压强（压力）σ** 和**接触面积 A**，二者相乘即可：

```
N = σ × A     （σ 单位 Pa 帕斯卡，A 单位 m² 平方米）
```

库直接支持这种输入方式：

```js
// 压强 100 kPa，接触面积 1 cm² = 1e-4 m² → N = 1e5 × 1e-4 = 10 N
RSF.computeFriction('steel', null, 0.5, { sigma: 1e5, area: 1e-4 });
```

### 3. 物体运动速度 → V

速度 `V` 单位 **m/s（米/秒）**，与库的 SI 单位制一致。注意换算：

| 常用单位 | 换算到 m/s |
|---|---|
| 1 mm/s | 1e-3 |
| 1 μm/s | 1e-6 |
| 1 cm/s | 1e-2 |
| 1 km/h | 0.2778 |

---

## 三、两种计算模式

### 库仑模式（简单，教学仿真推荐）

摩擦系数是常数，与速度无关：

```
滑动时：  F = μ_k · N
静止时：  F ≤ μ_s · N   （静摩擦，实际值由外力平衡决定）
```

```js
RSF.computeFriction('steel', 1000, 0.5);   // 滑动 → F = 0.42 × 1000 = 420 N
RSF.computeFriction('steel', 1000, 0);     // 静止 → 返回静摩擦上限 0.60 × 1000 = 600 N
```

### RSF 模式（精确，含速度依赖与粘滑）

摩擦系数随**速度**和**滑动历史（状态变量 θ）**变化，这是本库的核心：

```
μ(V,θ) = μ₀ + a·ln(V/V₀) + b·ln(V₀θ/Dc)
F = μ(V,θ) · N
```

能体现库仑模型无法描述的物理现象：
- **速度弱化**（`a−b<0`）：速度越快摩擦越小 → 可能发生粘滑；
- **速度强化**（`a−b≥0`）：速度越快摩擦越大 → 稳定滑动。

```js
const 慢 = RSF.computeFriction('granite', 1000, 1e-5).frictionForce; // 590.8 N
const 快 = RSF.computeFriction('granite', 1000, 1e-4).frictionForce; // 581.6 N
// 花岗岩是速度弱化材料：速度越快摩擦力越小（差约 9 N）
```

---

## 四、内置材质参数表（参考值）

> ⚠️ 库仑 `μ/μ_s` 为常见工程材料干摩擦的**教科书量级参考值**；RSF 参数（`mu0/a/b/Dc/V0`）为**岩石摩擦实验典型值**。不同批次材料差异很大，实际仿真建议用实测/标定值替换。

| 材质键 | 名称 | μ（动） | μ_s（静） | RSF 参数 |
|---|---|---|---|---|
| `granite` | 花岗岩（岩石） | 0.60 | 0.65 | μ₀=0.60, a=0.008, b=0.012, Dc=5μm |
| `sandstone` | 砂岩（岩石） | 0.55 | 0.60 | μ₀=0.60, a=0.005, b=0.010, Dc=10μm |
| `limestone` | 石灰岩（岩石） | 0.50 | 0.55 | μ₀=0.55, a=0.006, b=0.009, Dc=10μm |
| `steel` | 钢-钢（干） | 0.42 | 0.60 | —（库仑模式） |
| `aluminum` | 铝-钢（干） | 0.45 | 0.55 | — |
| `castiron` | 铸铁-铸铁（干） | 0.20 | 0.30 | — |
| `glass` | 玻璃-玻璃（干） | 0.90 | 0.95 | — |
| `wood` | 木材-木材 | 0.30 | 0.40 | — |
| `rubber` | 橡胶-混凝土（干） | 0.80 | 0.90 | — |
| `ptfe` | 聚四氟乙烯(PTFE)-钢 | 0.05 | 0.06 | — |
| `ice` | 冰-钢 | 0.03 | 0.10 | — |

> PTFE 和冰是极低摩擦材料的典型代表，适合做「减小摩擦」的对比实验。

---

## 五、嵌入仿真实验的完整示例

### 示例 1：最基本的仿真循环

每帧根据用户当前选择的三变量计算摩擦力：

```js
// 用户可调的三个变量
let 材质 = 'wood';        // 接触面材质（下拉框选项）
let 法向力 = 500;         // 压力（N）
let 速度 = 0.3;           // 当前速度（m/s）

function 更新摩擦力() {
  const r = RSF.computeFriction(材质, 法向力, 速度);
  return r.frictionForce;   // 摩擦力大小 (N)
}
```

### 示例 2：含方向与静摩擦的完整仿真

真实仿真中摩擦力方向**始终与运动方向相反**，且静止时受静摩擦约束：

```js
function 摩擦力矢量(材质, 法向力, 速度v) {
  const r = RSF.computeFriction(材质, 法向力, Math.abs(v));
  if (Math.abs(v) < 1e-6) {
    // 几乎静止：返回静摩擦上限，方向由外力决定（仿真器自行平衡）
    return { 大小: r.frictionForce, 是静摩擦: true };
  }
  // 滑动：方向与速度相反
  return { 大小: r.frictionForce, 是静摩擦: false, 方向: -Math.sign(v) };
}
```

### 示例 3：HTML 网页仿真（下拉框选材质）

```html
<select id="mat">
  <option value="steel">钢-钢（干）</option>
  <option value="wood">木材-木材</option>
  <option value="rubber">橡胶-混凝土</option>
  <option value="ptfe">聚四氟乙烯（特氟龙）</option>
  <option value="ice">冰-钢</option>
  <option value="granite">花岗岩</option>
</select>
压力 <input id="N" value="500"> N
速度 <input id="V" value="0.3"> m/s
<button onclick="calc()">计算</button>
<output id="out"></output>

<script src="rsf.js"></script>
<script>
  function calc() {
    const mat = document.getElementById('mat').value;
    const N   = parseFloat(document.getElementById('N').value);
    const V   = parseFloat(document.getElementById('V').value);
    const r   = RSF.computeFriction(mat, N, V);
    document.getElementById('out').textContent =
      r.material + '：μ=' + r.mu.toFixed(4) + '，摩擦力 F=' + r.frictionForce.toFixed(2) + ' N';
  }
</script>
```

---

## 六、进阶：体现粘滑等物理现象

如果仿真要体现**粘滑（stick-slip）**、**速度阶跃直接效应**、**静态愈合**，使用库的更底层接口（详见 `index.html` 演示页）：

| 现象 | 接口 | 说明 |
|---|---|---|
| 粘滑（锯齿形振荡） | `new RSF.StickSlipSlider({...})` | 需额外参数：弹簧刚度 k、加载速度 Vlp、法向力 N |
| 速度阶跃直接效应 | `RSF.velocityStepExperiment({...})` | 速度从 V₁ 跳到 V₂，μ 先跳变后松弛 |
| 静态对数愈合 | `RSF.healingCurve([10,100,1000])` | 静摩擦随静止时间对数增长 |

粘滑发生的判据（速度弱化 + 低刚度）：

```
粘滑条件：  a − b < 0  且  弹簧刚度 k < k_crit = N·(b − a) / Dc
```

---

## 七、注意事项与常见问题

1. **单位统一用 SI**：力 N、压强 Pa、速度 m/s、长度 m、时间 s。换算错误是最常见的坑。
2. **速度 V = 0（物体静止）**：RSF 的对数律在 V=0 发散，库已特殊处理——库仑模式返回静摩擦上限 `μ_s·N`；RSF 模式用极小速度 `1e-12` 近似稳态。仿真中静止物体的摩擦力应由外力平衡决定（≤ 静摩擦上限）。
3. **摩擦力是矢量**：`computeFriction` 返回的是**大小** `μ·N`，方向由仿真器按「与速度相反」处理。
4. **参考值需标定**：内置材质参数是量级参考值，正式实验请用实测数据；岩石的 a、b、Dc 尤其需要实验室摩擦实验标定。
5. **速度弱化 ≠ 一定粘滑**：还需满足刚度条件 `k < k_crit`（见第六节），否则仍是稳定滑动。
6. **精度**：库用自适应 Runge-Kutta 求解器，默认容差 `rtol=1e-9, atol=1e-11`，速度阶跃直接效应、稳态摩擦、静态愈合均已与解析解逐位吻合。

---

## 八、完整 API 一览

| 导出 | 用途 |
|---|---|
| `RSF.computeFriction(材质, N, V, opts)` | **三变量便捷接口**（本文核心） |
| `RSF.materials` | 内置材质参数表 |
| `RSF.RateStateFriction` | RSF 本构律类（μ、μ_ss、静摩擦、愈合曲线） |
| `RSF.SpringBlockSlider` | 准静态弹簧滑块（速度阶跃/愈合实验） |
| `RSF.StickSlipSlider` | 准动力粘滑仿真（复现粘滑） |
| `RSF.velocityStepExperiment` | 速度阶跃实验 |
| `RSF.healingCurve` | 静态愈合曲线 |
| `RSF.coulombForce(μ, N)` | 简单库仑摩擦 `μ·N` |
| `RSF.StateLaws` | 状态演化律（aging/slip/prz/nagata） |
